# 使用Alpine Linux作为基础镜像（更小）
FROM python:3.10-alpine

# 设置工作目录
WORKDIR /app

# 安装系统依赖（Alpine版本）
RUN apk add --no-cache \
    opencv \
    opencv-dev \
    gcc \
    g++ \
    musl-dev \
    linux-headers

# 复制requirements文件
COPY requirements-minimal.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements-minimal.txt

# 复制应用代码
COPY . .

# 设置环境变量
ENV PYTHONPATH=/app

# 暴露端口
EXPOSE 60000

# 启动命令
CMD ["python", "start_server_fixed.py"] 